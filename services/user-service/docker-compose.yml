services:
  postgres:
    image: postgres:latest
    environment:
      POSTGRES_PASSWORD: mysecretpassword
    ports:
      - "5435:5432"
    volumes:
      - user_service_pgdata:/var/lib/postgresql/data

volumes:
  user_service_pgdata:
