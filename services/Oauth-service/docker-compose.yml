services:
  postgres:
    image: postgres:latest
    container_name: oauth_service_postgres
    environment:
      POSTGRES_PASSWORD: mysecretpassword
    ports:
      - "5431:5432"
    volumes:
      - pgdata:/var/lib/postgresql

volumes: 
  pgdata:
  # pgdata_other: