services:
  postgres:
    image: postgres:latest
    environment:
      POSTGRES_PASSWORD: mysecretpassword
    ports:
      - "5437:5432"
    volumes:
      - tags_service_pgdata:/var/lib/postgresql/data

volumes:
  tags_service_pgdata:
